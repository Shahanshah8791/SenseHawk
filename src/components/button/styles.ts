import {StyleSheet} from 'react-native';

import Colors from '../../constants/Colors';

export const styles = StyleSheet.create({
  primaryButtonContainer: {
    width: '100%',
    justifyContent: 'center',
    alignItems: 'center',
    flexDirection: 'row',
    backgroundColor: Colors.color_1BAC4B,
    borderRadius: 24,
    paddingVertical: 16,
  },
  text16600: {
    fontSize: 16,
    fontWeight: '600',
  },
});
